/** Days without a balance update before an account is flagged as stale. */
export const STALE_BALANCE_DAYS = 30;

export const CASH_CURRENCIES = ["OMR", "USD", "EUR", "GBP", "AED", "HKD", "INR"] as const;
